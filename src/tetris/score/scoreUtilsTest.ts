import { levelProps } from './scoreUtis';

describe("score utils", () => {
  describe('foo', () => {
    it('bla', () => {
      console.log('lp', levelProps);
      
      expect(true).toBe(true);
    });
  });
});

